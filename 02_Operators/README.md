# 🐍 Day 02 – Python Operators

Welcome to **Day 02** of my Python Programming Learning Journey 🚀

Today, I learned and practiced **Python Operators**. Operators are symbols or keywords used to perform operations on variables and values.

---

## 📚 Topics Covered

### 1. Arithmetic Operators

Arithmetic operators are used to perform mathematical calculations.

| Operator | Name           | Example   |
| -------- | -------------- | --------- |
| `+`      | Addition       | `10 + 5`  |
| `-`      | Subtraction    | `10 - 5`  |
| `*`      | Multiplication | `10 * 5`  |
| `/`      | Division       | `10 / 5`  |
| `//`     | Floor Division | `10 // 3` |
| `%`      | Modulus        | `10 % 3`  |
| `**`     | Exponentiation | `10 ** 2` |

**File:** `arithmetic.py`

---

### 2. Relational Operators

Relational operators are used to compare two values.

| Operator | Meaning                  |
| -------- | ------------------------ |
| `==`     | Equal to                 |
| `!=`     | Not equal to             |
| `>`      | Greater than             |
| `<`      | Less than                |
| `>=`     | Greater than or equal to |
| `<=`     | Less than or equal to    |

**File:** `relational.py`

---

### 3. Logical Operators

Logical operators are used to combine conditions.

| Operator | Description                              |
| -------- | ---------------------------------------- |
| `and`    | True when both conditions are true       |
| `or`     | True when at least one condition is true |
| `not`    | Reverses the result                      |

**File:** `logical.py`

Example:

```python
age = 20
has_id = True

print(age >= 18 and has_id)
print(age >= 18 or has_id)
print(not has_id)
```

---

### 4. Assignment Operators

Assignment operators are used to assign and update values.

| Operator | Example   | Equivalent   |
| -------- | --------- | ------------ |
| `=`      | `a = 10`  | Assign       |
| `+=`     | `a += 5`  | `a = a + 5`  |
| `-=`     | `a -= 5`  | `a = a - 5`  |
| `*=`     | `a *= 5`  | `a = a * 5`  |
| `/=`     | `a /= 5`  | `a = a / 5`  |
| `//=`    | `a //= 5` | `a = a // 5` |
| `%=`     | `a %= 5`  | `a = a % 5`  |
| `**=`    | `a **= 5` | `a = a ** 5` |

**File:** `assignment.py`

---

## 📂 Files Created

```text
02_Operators/
│
├── arithmetic.py
├── relational.py
├── logical.py
└── assignment.py
```

---

## 💻 Practice

I practiced:

* Performing mathematical calculations
* Comparing values
* Combining multiple conditions
* Updating variable values
* Understanding different types of operators

---

## 🧠 What I Learned

* Operators are essential for performing operations in Python.
* Arithmetic operators are used for calculations.
* Relational operators return `True` or `False`.
* Logical operators help combine conditions.
* Assignment operators make it easier to update variables.

---

## 📈 Progress

**Day 02 – Python Operators:** ✅ Completed

```text
Day 01 – Python Basics       ✅
Day 02 – Operators           ✅
Day 03 – Conditional         ⬜
Day 04 – Loops               ⬜
Day 05 – Functions           ⬜
```

---

## 🔧 Git Commit

```bash
git add .
git commit -m "Complete Day 02 Python Operators"
git push origin main
```

---

## 🚀 Next Step

### Day 03 – Conditional Statements

Next, I will learn:

* `if`
* `if-else`
* `if-elif-else`
* Nested `if`
* Conditional expressions

**Keep Learning → Keep Practicing → Keep Building 🚀**
